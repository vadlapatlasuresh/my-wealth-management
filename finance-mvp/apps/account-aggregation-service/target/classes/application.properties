spring.application.name=account-aggregation-service
server.port=8082

# Active profile: dev uses in-memory H2 (see application-dev.properties)
spring.profiles.active=dev

# Plaid Configuration (sandbox keys — rotate for production)
plaid.client-id=6a2208ca85915c000e103337
plaid.secret=d27417dbcbb1634051b1f7f76b4585
plaid.env=sandbox
plaid.client-name=My Wealth Management

# JWT Configuration (must match auth-service)
jwt.secret=THIS_IS_A_VERY_LONG_AND_SECURE_SECRET_KEY_FOR_JWT_AUTHENTICATION_DEMO_PURPOSES_ONLY_REPLACE_IN_PRODUCTION
jwt.expiration=86400000

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
