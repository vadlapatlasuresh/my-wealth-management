<?xml version="1.0" encoding="utf-8"?>
<!--
  Permits plain HTTP (cleartext) ONLY to local development hosts so the app can
  reach a dev API gateway while testing on an emulator/device:
    10.0.2.2  -> the host machine from the Android emulator
    localhost / 127.0.0.1 -> the device itself
  Everything else still requires HTTPS. For production, point the app at an HTTPS
  gateway (VITE_API_BASE) — no cleartext needed.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
